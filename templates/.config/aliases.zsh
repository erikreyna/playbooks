# clokta aliases
alias celex="clokta -p elections && export AWS_PROFILE=elections"
alias cmain="clokta -p main && export AWS_PROFILE=main"
alias cnews="clokta -p news-eng && export AWS_PROFILE=news-eng"
alias cpub="clokta -p pub && export AWS_PROFILE=pub"
alias csand="clokta -p sandbox && export AWS_PROFILE=sandbox"

# docker compose aliases
alias dco="docker compose"